{
  "mcpServers": {
    "python-vibe": {
      "type": "stdio",
      "command": "python3",
      "args": ["-m", "harness", "mcp", "--project", "${workspaceFolder}"],
      "env": {
        "PYTHONPATH": "${workspaceFolder}/src"
      }
    }
  }
}
