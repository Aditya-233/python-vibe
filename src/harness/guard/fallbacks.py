"""Fixed reply when the guard blocks twice. Never invent a secret."""

PYTHON_VIBE_FALLBACK = (
    "I can't ship that draft. Ask for a smaller local script "
    "(stdlib, no secrets, no curl|sh) and I will try again."
)
